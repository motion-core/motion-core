---
name: Video Player
description: A refined video player component with a progress slider and smooth GSAP-powered interactions.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import videoPlayerSource
	from "motion-core/components/video-player/VideoPlayer.svelte?raw"; import
	videoSliderSource from
	"motion-core/components/video-player/VideoSlider.svelte?raw"; import gsapHelperSource
	from "motion-core/helpers/gsap.ts?raw"; import
	VideoPlayerDemo from "./VideoPlayerDemo.svelte"; import videoPlayerDemoSource
	from "./VideoPlayerDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="video-player" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { VideoPlayer } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["video-player"]}
	sources={[
		{ name: "Example", code: videoPlayerDemoSource, language: "svelte" },
		{ name: "VideoPlayer.svelte", code: videoPlayerSource, language: "svelte" },
		{ name: "VideoSlider.svelte", code: videoSliderSource, language: "svelte" },
		{ name: "helpers/gsap.ts", code: gsapHelperSource, language: "typescript" },
	]}
>
	{#snippet children(props)}
		<VideoPlayerDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### VideoPlayer

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "src",
			type: "string",
			default: "undefined",
			description: "The source URL of the video file.",
		},
		{
			prop: "poster",
			type: "string",
			default: "undefined",
			description: "An image URL to be shown while the video is downloading.",
		},
		{
			prop: "autoplay",
			type: "boolean",
			default: "false",
			description:
				"Whether the video should start playing as soon as it is ready.",
		},
		{
			prop: "muted",
			type: "boolean",
			default: "true",
			description: "Whether the audio should be muted by default.",
		},
		{
			prop: "loop",
			type: "boolean",
			default: "false",
			description:
				"Whether the video should start over again every time it is finished.",
		},
		{
			prop: "hideControls",
			type: "boolean",
			default: "false",
			description:
				"Whether to permanently hide controls and disable interaction.",
		},
		{
			prop: "class",
			type: "string",
			default: "undefined",
			description: "Additional CSS classes for the container.",
		},
	]}
/>
