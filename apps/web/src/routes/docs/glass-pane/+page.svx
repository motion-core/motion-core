---
name: Glass Pane
description: An image-backed refractive view with animated diagonal glass panels, chromatic aberration, and specular highlights.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import glassPaneSource from
	"motion-core/components/glass-pane/GlassPane.svelte?raw"; import sceneSource
	from "motion-core/components/glass-pane/GlassPaneScene.svelte?raw"; import
	GlassPaneDemo from "./GlassPaneDemo.svelte"; import glassPaneDemoSource from
	"./GlassPaneDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="glass-pane" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { GlassPane } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["glass-pane"]}
	sources={[
		{ name: "Example", code: glassPaneDemoSource, language: "svelte" },
		{ name: "GlassPane.svelte", code: glassPaneSource, language: "svelte" },
		{ name: "GlassPaneScene.svelte", code: sceneSource, language: "svelte" },
	]}
>
	{#snippet children(props)}
		<GlassPaneDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### GlassPane

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "image",
			type: "string",
			default: "—",
			description: "URL of the image to be used as the background.",
		},
		{
			prop: "rotation",
			type: "number",
			default: "0",
			description: "Rotation of the glass field in degrees.",
		},
		{
			prop: "refraction",
			type: "number",
			default: "1",
			description: "Strength of the glass refraction.",
		},
		{
			prop: "chromaticAberration",
			type: "number",
			default: "1",
			description: "Strength of the chromatic aberration (RGB shift).",
		},
		{
			prop: "panelWidth",
			type: "number",
			default: "0.82",
			description: "Width of the diagonal glass panels.",
		},
		{
			prop: "waveFrequency",
			type: "number",
			default: "0.0",
			description: "Frequency of the panel wave.",
		},
		{
			prop: "waveAmplitude",
			type: "number",
			default: "0.0",
			description: "Amplitude of the panel wave.",
		},
		{
			prop: "speed",
			type: "number",
			default: "0.65",
			description: "Animation speed multiplier.",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Additional CSS classes for the container.",
		},
	]}
/>
