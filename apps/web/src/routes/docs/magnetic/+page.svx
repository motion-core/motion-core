---
name: Magnetic
description: A wrapper that applies spring physics to elements, making them stick to the cursor on hover.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import magneticSource from
	"motion-core/components/magnetic/Magnetic.svelte?raw"; import MagneticDemo
	from "./MagneticDemo.svelte"; import magneticDemoSource from
	"./MagneticDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="magnetic" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { Magnetic } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["magnetic"]}
	refreshOnControlChange
	sources={[
		{ name: "Example", code: magneticDemoSource, language: "svelte" },
		{ name: "Magnetic.svelte", code: magneticSource, language: "svelte" },
	]}
>
	{#snippet children(props)}
		<MagneticDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### Magnetic

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "duration",
			type: "number",
			default: "1",
			description: "Duration of the magnetic movement.",
		},
		{
			prop: "ease",
			type: "string",
			default: '"elastic.out(1, 0.3)"',
			description: "GSAP easing function for the movement.",
		},
		{
			prop: "class",
			type: "string",
			default: "—",
			description: "Additional CSS classes for the wrapper.",
		},
	]}
/>
