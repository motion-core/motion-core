---
name: MacOS Dock
description: A navigation bar that simulates magnification physics and smooth easing on hover.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import macosDockSource from
	"motion-core/components/macos-dock/MacosDock.svelte?raw"; import MacosDockDemo
	from "./MacosDockDemo.svelte"; import macosDockDemoSource from
	"./MacosDockDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="macos-dock" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { MacosDock } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["macos-dock"]}
	sources={[
		{ name: "Example", code: macosDockDemoSource, language: "svelte" },
		{ name: "MacosDock.svelte", code: macosDockSource, language: "svelte" },
	]}
>
	{#snippet children(props)}
		<MacosDockDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### MacosDock

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "items",
			type: "DockItem[]",
			default: "—",
			description: "Array of items to display { src, alt, label, href }.",
		},
		{
			prop: "baseWidth",
			type: "number",
			default: "4",
			description: "Base width of each icon in em units.",
		},
		{
			prop: "magnification",
			type: "number",
			default: "1.5",
			description: "Magnification factor for the hovered icon.",
		},
		{
			prop: "distance",
			type: "number",
			default: "3",
			description: "Number of adjacent icons affected by the magnification.",
		},
		{
			prop: "class",
			type: "string",
			default: "—",
			description: "Additional CSS classes for the nav container.",
		},
	]}
/>
