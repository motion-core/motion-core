---
name: Halo
description: Atmospheric scattering (Rayleigh & Mie) based halo effect with dynamic camera rotation and sun light simulation.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte";
	import InstallationTabs from "$lib/components/docs/LegacyInstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte";
	import Step from "$lib/components/docs/markdown/Step.svelte";

	import haloSource from "motion-core/components/halo/Halo.svelte?raw";
	import sceneSource from "motion-core/components/halo/HaloScene.svelte?raw";
	import colorHelperSource from "motion-core/helpers/color.ts?raw";
	import HaloDemo from "./HaloDemo.svelte";
	import haloDemoSource from "./HaloDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="halo" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { Halo } from "$lib/motion-core";
```

	</Step>
</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["halo"]}
	sources={[
		{ name: "Example", code: haloDemoSource, language: "svelte" },
		{
			name: "Halo.svelte",
			code: haloSource,
			language: "svelte",
		},
		{ name: "HaloScene.svelte", code: sceneSource, language: "svelte" },
		{ name: "helpers/color.ts", code: colorHelperSource, language: "typescript" },
	]}
>
	{#snippet children(props)}
		<HaloDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### Halo

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "scale",
			type: "number",
			default: "1",
			description: "Scale multiplier for the halo field.",
		},
		{
			prop: "offsetX",
			type: "number",
			default: "0",
			description: "Horizontal halo offset in normalized viewport units.",
		},
		{
			prop: "offsetY",
			type: "number",
			default: "0",
			description: "Vertical halo offset in normalized viewport units.",
		},
		{
			prop: "rotation",
			type: "number",
			default: "0",
			description: "Halo rotation in degrees.",
		},
		{
			prop: "rotationSpeed",
			type: "number",
			default: "0.5",
			description: "Camera rotation speed multiplier.",
		},
			{
				prop: "backgroundColor",
				type: 'string | number | [number, number, number] | { r: number; g: number; b: number }',
				default: '"#17181A"',
				description: "Color of the background behind the scattering effect.",
			},
		{
			prop: "cameraDistance",
			type: "number",
			default: "3.0",
			description: "Distance of the camera from the center.",
		},
		{
			prop: "sunX",
			type: "number",
			default: "0.0",
			description: "Sun light direction vector (X component).",
		},
		{
			prop: "sunY",
			type: "number",
			default: "0.0",
			description: "Sun light direction vector (Y component).",
		},
		{
			prop: "sunZ",
			type: "number",
			default: "1.0",
			description: "Sun light direction vector (Z component).",
		},
		{
			prop: "intensity",
			type: "number",
			default: "1.0",
			description: "Overall intensity/brightness multiplier for the scattering effect.",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Additional CSS classes for the container.",
		},
	]}
/>
