---
name: Plasma Grid
description: A fluid, pixelated noise pattern useful for backgrounds.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import plasmaGridSource from
	"motion-core/components/plasma-grid/PlasmaGrid.svelte?raw"; import sceneSource
	from "motion-core/components/plasma-grid/PlasmaGridScene.svelte?raw"; import
	colorHelperSource from "motion-core/helpers/color.ts?raw"; import
	PlasmaGridDemo from "./PlasmaGridDemo.svelte"; import plasmaGridDemoSource
	from "./PlasmaGridDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="plasma-grid" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { PlasmaGrid } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["plasma-grid"]}
	sources={[
		{ name: "Example", code: plasmaGridDemoSource, language: "svelte" },
		{ name: "PlasmaGrid.svelte", code: plasmaGridSource, language: "svelte" },
		{ name: "PlasmaGridScene.svelte", code: sceneSource, language: "svelte" },
		{ name: "helpers/color.ts", code: colorHelperSource, language: "typescript" },
	]}
>
	{#snippet children(props)}
		<PlasmaGridDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### PlasmaGrid

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "color",
			type: 'string | number | [number, number, number] | { r: number; g: number; b: number }',
			default: '"#17181A"',
			description: "The base background color of the effect.",
		},
		{
			prop: "highlightColor",
			type: 'string | number | [number, number, number] | { r: number; g: number; b: number }',
			default: '"#FF6900"',
			description: "The color used for the plasma noise gradients.",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Additional CSS classes for the container.",
		},
	]}
/>
