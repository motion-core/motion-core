---
name: Image Trail
description: An interactive effect that spawns a trail of images following the cursor with scaling and rotation.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import imageTrailSource from
	"motion-core/components/image-trail/ImageTrail.svelte?raw"; import
	ImageTrailDemo from "./ImageTrailDemo.svelte"; import imageTrailDemoSource
	from "./ImageTrailDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="image-trail" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { ImageTrail } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["image-trail"]}
	sources={[
		{ name: "Example", code: imageTrailDemoSource, language: "svelte" },
		{ name: "ImageTrail.svelte", code: imageTrailSource, language: "svelte" },
	]}
>
	{#snippet children(props)}
		<ImageTrailDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### ImageTrail

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "images",
			type: "string[]",
			default: "—",
			description: "Ordered list of image URLs cycled for the trail.",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Adds classes to the wrapping div.",
		},
		{
			prop: "config",
			type: "TrailConfig",
			default: "DEFAULT_CONFIG",
			description: "Optional overrides for animation behaviour.",
		},
	]}
/>

### TrailConfig

<ApiTable
	headers={["Key", "Type", "Default"]}
	keys={["key", "type", "default"]}
	data={[
		{
			key: "imageLifespan",
			type: "number",
			default: "600",
			description: "Duration (ms) each image stays before removal starts.",
		},
		{
			key: "removalTickMs",
			type: "number",
			default: "16",
			description: "Interval (ms) between cleanup checks.",
		},
		{
			key: "mouseThreshold",
			type: "number",
			default: "40",
			description:
				"Distance (px) pointer must travel before spawning next image.",
		},
		{
			key: "minMovementForImage",
			type: "number",
			default: "5",
			description: "Minimum motion to consider the pointer “moving”.",
		},
		{
			key: "inDuration",
			type: "number",
			default: "600",
			description: "Scale-in animation duration (ms).",
		},
		{
			key: "outDuration",
			type: "number",
			default: "800",
			description: "Scale-out animation duration (ms).",
		},
		{
			key: "maxRotationFactor",
			type: "number",
			default: "3",
			description: "Scales random rotation range based on pointer speed.",
		},
		{
			key: "baseRotation",
			type: "number",
			default: "30",
			description: "Maximum base rotation in degrees when speed factor is 1.",
		},
		{
			key: "speedSmoothingFactor",
			type: "number",
			default: "0.25",
			description: "Eases pointer speed readings for steadier animation.",
		},
		{
			key: "minImageSize",
			type: "number",
			default: "260",
			description: "Smallest image size in pixels.",
		},
		{
			key: "maxImageSize",
			type: "number",
			default: "340",
			description: "Largest image size in pixels.",
		},
		{
			key: "staggerOut",
			type: "number",
			default: "40",
			description: "Delay (ms) between each image’s exit animation.",
		},
	]}
/>
