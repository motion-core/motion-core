---
name: Preloader
description: A kinetic loading sequence that decelerates an image strip before scaling the hero asset to full screen.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import preloaderSource from
	"motion-core/components/preloader/Preloader.svelte?raw"; import PreloaderDemo
	from "./PreloaderDemo.svelte"; import preloaderDemoSource from
	"./PreloaderDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="preloader" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { Preloader } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	sources={[
		{ name: "Example", code: preloaderDemoSource, language: "svelte" },
		{ name: "Preloader.svelte", code: preloaderSource, language: "svelte" },
	]}
>
	<PreloaderDemo />
</ComponentPreview>

## Props

### Preloader

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "images",
			type: "{src: string, alt?: string}[]",
			default: "—",
			description: "Array of image objects (ideally 5 for best look).",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Additional CSS classes for the container.",
		},
		{
			prop: "onComplete",
			type: "() => void",
			default: "undefined",
			description: "Callback function when animation finishes.",
		},
	]}
/>
