---
name: Lava Lamp
description: A smooth and organic lava lamp effect using raymarching shaders and SDFs.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import lavaLampSource from
	"motion-core/components/lava-lamp/LavaLamp.svelte?raw"; import sceneSource
	from "motion-core/components/lava-lamp/LavaLampScene.svelte?raw"; import
	colorHelperSource from "motion-core/helpers/color.ts?raw"; import
	LavaLampDemo from "./LavaLampDemo.svelte"; import lavaLampDemoSource from
	"./LavaLampDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="lava-lamp" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { LavaLamp } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["lava-lamp"]}
	sources={[
		{ name: "Example", code: lavaLampDemoSource, language: "svelte" },
		{
			name: "LavaLamp.svelte",
			code: lavaLampSource,
			language: "svelte",
		},
		{
			name: "LavaLampScene.svelte",
			code: sceneSource,
			language: "svelte",
		},
		{ name: "helpers/color.ts", code: colorHelperSource, language: "typescript" },
	]}
>
	{#snippet children(props)}
		<LavaLampDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### LavaLamp

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "scale",
			type: "number",
			default: "1",
			description: "Scale multiplier for the lava field.",
		},
		{
			prop: "offsetX",
			type: "number",
			default: "0",
			description: "Horizontal lava offset in normalized viewport units.",
		},
		{
			prop: "offsetY",
			type: "number",
			default: "0",
			description: "Vertical lava offset in normalized viewport units.",
		},
		{
			prop: "rotation",
			type: "number",
			default: "0",
			description: "Lava field rotation in degrees.",
		},
		{
			prop: "color",
			type: "string",
			default: '"#17181A"',
			description: "Base color of the lava blobs.",
		},
		{
			prop: "fresnelColor",
			type: "string",
			default: '"#ff6900"',
			description: "Color of the rim light (fresnel effect).",
		},
		{
			prop: "speed",
			type: "number",
			default: "1.0",
			description: "Animation speed multiplier.",
		},
		{
			prop: "fresnelPower",
			type: "number",
			default: "3.0",
			description: "Strength of the fresnel effect (rim lighting sharpness).",
		},
		{
			prop: "radius",
			type: "number",
			default: "1",
			description: "Global radius multiplier for the lava blobs.",
		},
		{
			prop: "smoothness",
			type: "number",
			default: "0.1",
			description: "Smoothness of the blob blending (metaball effect).",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Additional CSS classes for the container.",
		},
	]}
/>
