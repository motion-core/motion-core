---
name: Pixelated Image
description: A media revealer that animates blocky pixel grids which progressively sharpen to full resolution.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import pixelatedImageSource
	from "motion-core/components/pixelated-image/PixelatedImage.svelte?raw";
	import sceneSource from
	"motion-core/components/pixelated-image/PixelatedImageScene.svelte?raw";
	import PixelatedImageDemo from "./PixelatedImageDemo.svelte"; import
	pixelatedImageDemoSource from "./PixelatedImageDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="pixelated-image" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { PixelatedImage } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["pixelated-image"]}
	sources={[
		{ name: "Example", code: pixelatedImageDemoSource, language: "svelte" },
		{
			name: "PixelatedImage.svelte",
			code: pixelatedImageSource,
			language: "svelte",
		},
		{
			name: "PixelatedImageScene.svelte",
			code: sceneSource,
			language: "svelte",
		},
	]}
>
	{#snippet children(props)}
		<PixelatedImageDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### PixelatedImage

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "src",
			type: "string",
			default: "—",
			description: "URL of the image to be displayed.",
		},
		{
			prop: "initialGridSize",
			type: "number",
			default: "6.0",
			description:
				"Initial size of the pixel grid (number of vertical blocks).",
		},
		{
			prop: "stepDuration",
			type: "number",
			default: "0.15",
			description: "Duration (in seconds) of each pixelation step.",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Additional CSS classes for the container.",
		},
	]}
/>
