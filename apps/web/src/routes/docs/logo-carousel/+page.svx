---
name: Logo Carousel
description: A multi-column display that shuffles brand logos infinitely with vertical motion and fade transitions.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import logoCarouselSource
	from "motion-core/components/logo-carousel/LogoCarousel.svelte?raw"; import
	logoColumnSource from
	"motion-core/components/logo-carousel/LogoColumn.svelte?raw"; import
	LogoCarouselDemo from "./LogoCarouselDemo.svelte"; import
	logoCarouselDemoSource from "./LogoCarouselDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="logo-carousel" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { LogoCarousel } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["logo-carousel"]}
	refreshOnControlChange
	sources={[
		{ name: "Example", code: logoCarouselDemoSource, language: "svelte" },
		{
			name: "LogoCarousel.svelte",
			code: logoCarouselSource,
			language: "svelte",
		},
		{ name: "LogoColumn.svelte", code: logoColumnSource, language: "svelte" },
	]}
>
	{#snippet children(props)}
		<LogoCarouselDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### LogoCarousel

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "logos",
			type: "Logo[]",
			default: "—",
			description: "Array of logo objects { name, id, component }.",
		},
		{
			prop: "columnCount",
			type: "number",
			default: "2",
			description: "Number of columns to distribute logos into.",
		},
		{
			prop: "cycleInterval",
			type: "number",
			default: "2000",
			description: "Time in milliseconds between logo transitions.",
		},
		{
			prop: "class",
			type: "string",
			default: "—",
			description: "Additional CSS classes.",
		},
	]}
/>
