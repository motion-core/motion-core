---
name: Globe
description: A Fresnel-lit globe with rim glow, haloed atmosphere, land mesh detail, and marker layers for data storytelling.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/LegacyInstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import globeSource from
	"motion-core/components/globe/Globe.svelte?raw"; import globeSceneSource from
	"motion-core/components/globe/GlobeScene.svelte?raw"; import
	globeMarkerItemSource from
	"motion-core/components/globe/GlobeMarkerItem.svelte?raw"; import typesSource
	from "motion-core/components/globe/types.ts?raw"; import colorHelperSource from
	"motion-core/helpers/color.ts?raw"; import GlobeDemo from "./GlobeDemo.svelte";
	import globeDemoSource from "./GlobeDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="globe" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { Globe } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["globe"]}
	sources={[
		{ name: "Example", code: globeDemoSource, language: "svelte" },
		{ name: "Globe.svelte", code: globeSource, language: "svelte" },
		{ name: "GlobeScene.svelte", code: globeSceneSource, language: "svelte" },
		{
			name: "GlobeMarkerItem.svelte",
			code: globeMarkerItemSource,
			language: "svelte",
		},
		{ name: "types.ts", code: typesSource, language: "typescript" },
		{ name: "helpers/color.ts", code: colorHelperSource, language: "typescript" },
	]}
>
	{#snippet children(props)}
		<GlobeDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### Globe

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "scale",
			type: "number",
			default: "1",
			description: "Scale multiplier for the globe field.",
		},
		{
			prop: "offsetX",
			type: "number",
			default: "0",
			description: "Horizontal globe offset in normalized viewport units.",
		},
		{
			prop: "offsetY",
			type: "number",
			default: "0",
			description: "Vertical globe offset in normalized viewport units.",
		},
		{
			prop: "rotation",
			type: "number",
			default: "0",
			description: "Globe field rotation in degrees.",
		},
		{
			prop: "fresnelConfig",
			type: "FresnelConfig",
			default: "Default Fresnel",
			description: "Overrides for the Fresnel shader uniforms.",
		},
		{
			prop: "atmosphereConfig",
			type: "AtmosphereConfig",
			default: "Default Atmosphere",
			description: "Configuration for the atmospheric halo glow.",
		},
		{
			prop: "pointCount",
			type: "number",
			default: "15000",
			description: "Total number of points generated for land sampling.",
		},
		{
			prop: "landPointColor",
			type: "string | number | readonly [number, number, number] | { r: number; g: number; b: number }",
			default: '"#f77114"',
			description: "Color of the dots representing land.",
		},
		{
			prop: "pointSize",
			type: "number",
			default: "0.05",
			description: "Size of each land dot.",
		},
		{
			prop: "autoRotate",
			type: "boolean",
			default: "true",
			description: "Whether the globe should auto-rotate.",
		},
		{
			prop: "lockedPolarAngle",
			type: "boolean",
			default: "true",
			description: "Whether to lock the vertical view angle.",
		},
		{
			prop: "markers",
			type: "GlobeMarker[]",
			default: "[]",
			description: "Array of markers to display on the globe.",
		},
		{
			prop: "markerTooltip",
			type: "Snippet<[GlobeMarkerTooltipContext]>",
			default: "undefined",
			description:
				"Custom tooltip renderer for markers. Receives marker data and visibility context.",
		},
		{
			prop: "focusOn",
			type: "[number, number] | null",
			default: "null",
			description: "Coordinates [lat, lon] to focus the camera on.",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Additional CSS classes for the container.",
		},
	]}
/>

### GlobeMarkerTooltipContext

<ApiTable
	headers={["Key", "Type", "Default"]}
	keys={["key", "type", "default"]}
	data={[
		{
			key: "marker",
			type: "GlobeMarker",
			default: "-",
			description: "Marker currently being rendered.",
		},
		{
			key: "index",
			type: "number",
			default: "-",
			description: "Marker index in the markers array.",
		},
		{
			key: "visibility",
			type: "number",
			default: "-",
			description: "Visibility factor in range [0,1] based on camera-facing side.",
		},
	]}
/>

### FresnelConfig

<ApiTable
	headers={["Key", "Type", "Default"]}
	keys={["key", "type", "default"]}
	data={[
		{
			key: "color",
			type: "string | number | readonly [number, number, number] | { r: number; g: number; b: number }",
			default: '"#17181A"',
			description: "Base color before the rim glow is applied.",
		},
		{
			key: "rimColor",
			type: "string | number | readonly [number, number, number] | { r: number; g: number; b: number }",
			default: '"#FF6900"',
			description: "Color tint used for the Fresnel rim.",
		},
		{
			key: "rimPower",
			type: "number",
			default: "6",
			description: "Controls how tightly the rim hugs the sphere.",
		},
		{
			key: "rimIntensity",
			type: "number",
			default: "1.5",
			description: "Multiplier applied to the rim color.",
		},
	]}
/>

### AtmosphereConfig

<ApiTable
	headers={["Key", "Type", "Default"]}
	keys={["key", "type", "default"]}
	data={[
		{
			key: "color",
			type: "string | number | readonly [number, number, number] | { r: number; g: number; b: number }",
			default: '"#FF6900"',
			description: "Color of the atmosphere glow.",
		},
		{
			key: "scale",
			type: "number",
			default: "1.1",
			description: "Size of the atmosphere mesh relative to the globe radius.",
		},
		{
			key: "power",
			type: "number",
			default: "12.0",
			description:
				"Falloff exponent of the glow. Higher values mean a sharper edge.",
		},
		{
			key: "coefficient",
			type: "number",
			default: "0.9",
			description:
				"Base coefficient for intensity calculation. Controls inward extent.",
		},
		{
			key: "intensity",
			type: "number",
			default: "1.1",
			description: "Global intensity multiplier for the glow.",
		},
	]}
/>

### GlobeMarker

<ApiTable
	headers={["Key", "Type", "Default"]}
	keys={["key", "type", "default"]}
	data={[
		{
			key: "location",
			type: "[number, number]",
			default: "-",
			description: "Latitude and Longitude coordinates.",
		},
		{
			key: "size",
			type: "number",
			default: "0.05",
			description: "Base size scale of the marker.",
		},
		{
			key: "color",
			type: "string",
			default: '"#ffffff"',
			description: "Color of the marker point.",
		},
		{
			key: "label",
			type: "string",
			default: "undefined",
			description: "Fallback tooltip text when no custom renderer is passed.",
		},
	]}
/>
