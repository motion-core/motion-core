---
name: Split Hover
description: A text effect that splits characters and animates stacked layers on hover for a seamless swap.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import splitHoverSource from
	"motion-core/components/split-hover/SplitHover.svelte?raw"; import gsapHelperSource
	from "motion-core/helpers/gsap.ts?raw"; import
	SplitHoverDemo from "./SplitHoverDemo.svelte"; import splitHoverDemoSource
	from "./SplitHoverDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="split-hover" />
	</Step>
		<Step title="Import the component">
			Import the component into your Svelte file:

```ts
import { SplitHover } from "$lib/motion-core";
```

    	</Step>

</Steps>

## Usage

<ComponentPreview
	sources={[
		{ name: "Example", code: splitHoverDemoSource, language: "svelte" },
		{ name: "SplitHover.svelte", code: splitHoverSource, language: "svelte" },
		{ name: "helpers/gsap.ts", code: gsapHelperSource, language: "typescript" },
	]}
>
	<SplitHoverDemo />
</ComponentPreview>

## Props

### SplitHover

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "hoverTarget",
			type: "HTMLElement | null",
			default: "null",
			description:
				"Optional element that should trigger hover events (useful for buttons wrapping the text).",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Additional classes for the wrapper.",
		},
	]}
/>
