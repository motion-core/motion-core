---
name: Marquee
description: A scrolling content loop that accelerates, reverses, or syncs with scroll position.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import MarqueeDemo from
	"./MarqueeDemo.svelte"; import marqueeDemoSource from
	"./MarqueeDemo.svelte?raw"; import marqueeSource from
	"motion-core/components/marquee/Marquee.svelte?raw"; import gsapHelperSource from
	"motion-core/helpers/gsap.ts?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="marquee" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { Marquee } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["marquee"]}
	refreshOnControlChange
	sources={[
		{ name: "Example", code: marqueeDemoSource, language: "svelte" },
		{ name: "Marquee.svelte", code: marqueeSource, language: "svelte" },
		{ name: "helpers/gsap.ts", code: gsapHelperSource, language: "typescript" },
	]}
>
	{#snippet children(props)}
		<MarqueeDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### Marquee

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "children",
			type: "Snippet",
			default: "—",
			description: "The content of the marquee.",
		},
		{
			prop: "duration",
			type: "number",
			default: "5",
			description: "The time in seconds for one full loop cycle.",
		},
		{
			prop: "velocity",
			type: "number",
			default: "0.5",
			description:
				"Sensitivity to scroll velocity. Higher numbers make it speed up more when scrolling.",
		},
		{
			prop: "repeat",
			type: "number",
			default: "3",
			description:
				"Number of times to duplicate the content for seamless looping.",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Classes for the outer container.",
		},
		{
			prop: "gap",
			type: "number",
			default: "32",
			description:
				"Gap between items and horizontal padding for the track (px).",
		},
		{
			prop: "reversed",
			type: "boolean",
			default: "false",
			description: "Whether to reverse the animation direction.",
		},
		{
			prop: "scrollElement",
			type: "string | HTMLElement | null",
			default: "window",
			description:
				"Optional selector or element for the scroll container. Useful when scrolling within a specific div instead of the window.",
		},
	]}
/>
