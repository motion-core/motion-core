---
name: Glitter Cloth
description: Animated silk-like cloth shader with fine glitter noise and subtle vignette depth shading.
---

<script lang="ts">
	import ComponentPreview from "$lib/components/docs/ComponentPreview.svelte";
	import { componentPreviewControls } from "$lib/components/docs/component-preview/presets";
	import ApiTable from "$lib/components/docs/ApiTable.svelte"; import
	InstallationTabs from "$lib/components/docs/InstallationTabs.svelte";
	import Steps from "$lib/components/docs/markdown/Steps.svelte"; import Step
	from "$lib/components/docs/markdown/Step.svelte"; import glitterClothSource
	from "motion-core/components/glitter-cloth/GlitterCloth.svelte?raw"; import
	sceneSource from
	"motion-core/components/glitter-cloth/GlitterClothScene.svelte?raw"; import
	colorHelperSource from "motion-core/helpers/color.ts?raw"; import
	GlitterClothDemo from "./GlitterClothDemo.svelte"; import
	glitterClothDemoSource from "./GlitterClothDemo.svelte?raw";
</script>

## Installation

<Steps>
	<Step title="Install the component">
		Run the following command to install the component and its dependencies:
		<InstallationTabs component="glitter-cloth" />
	</Step>
	<Step title="Import the component">
		Import the component into your Svelte file:

```ts
import { GlitterCloth } from "$lib/motion-core";
```

    </Step>

</Steps>

## Usage

<ComponentPreview
	controls={componentPreviewControls["glitter-cloth"]}
	sources={[
		{ name: "Example", code: glitterClothDemoSource, language: "svelte" },
		{
			name: "GlitterCloth.svelte",
			code: glitterClothSource,
			language: "svelte",
		},
		{ name: "GlitterClothScene.svelte", code: sceneSource, language: "svelte" },
		{ name: "helpers/color.ts", code: colorHelperSource, language: "typescript" },
	]}
>
	{#snippet children(props)}
		<GlitterClothDemo {...props} />
	{/snippet}
</ComponentPreview>

## Props

### GlitterCloth

<ApiTable
	headers={["Prop", "Type", "Default"]}
	keys={["prop", "type", "default"]}
	data={[
		{
			prop: "color",
			type: "string | number | [number, number, number] | { r: number; g: number; b: number }",
			default: '"#FF6900"',
			description:
				"Primary color used for the shader palette. Accent and shadow tones are derived automatically.",
		},
		{
			prop: "speed",
			type: "number",
			default: "1.0",
			description: "Speed multiplier for the full shader animation timeline.",
		},
		{
			prop: "brightness",
			type: "number",
			default: "1.0",
			description: "Global intensity multiplier for the base silk color.",
		},
		{
			prop: "blendStrength",
			type: "number",
			default: "0.02",
			description: "Opacity of the vivid-light glitter blend.",
		},
		{
			prop: "noiseScale",
			type: "number",
			default: "4.0",
			description:
				"Spatial scale for simplex noise sampling. Lower values create finer glitter.",
		},
		{
			prop: "vignetteStrength",
			type: "number",
			default: "15.0",
			description: "Base strength of vignette falloff.",
		},
		{
			prop: "vignettePower",
			type: "number",
			default: "0.25",
			description:
				"Vignette curve exponent. Lower values produce a softer rolloff.",
		},
		{
			prop: "vignetteOpacity",
			type: "number",
			default: "1.0",
			description:
				"Opacity of the vignette effect (`0` disables it, `1` is full effect).",
		},
		{
			prop: "class",
			type: "string",
			default: '""',
			description: "Additional CSS classes for the container.",
		},
	]}
/>
